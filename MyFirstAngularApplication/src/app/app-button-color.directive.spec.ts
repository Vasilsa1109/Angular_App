import { AppButtonColorDirective } from './app-button-color.directive';

describe('AppButtonColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppButtonColorDirective();
    expect(directive).toBeTruthy();
  });
});
